# Mineradio Nginx 配置 (CentOS 7)
# 安装: yum install -y nginx
# 放置: /etc/nginx/conf.d/mineradio.conf
# 重启: systemctl restart nginx

server {
    listen 80;
    server_name _;  # 替换为你的域名，如 mineradio.yourdomain.com

    # 静态资源缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        root /opt/mineradio/public;
        expires 30d;
        add_header Cache-Control "public, immutable";
        try_files $uri $uri/ =404;
    }

    # PWA 文件
    location ~ ^/(manifest\.json|sw\.js)$ {
        root /opt/mineradio/public;
        add_header Cache-Control "no-cache";
        try_files $uri $uri/ =404;
    }

    # API 和主应用 - 反向代理到 Node.js
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 86400;
    }

    # 大文件上传限制
    client_max_body_size 50M;
}

# HTTPS 配置（获取证书后启用）
# server {
#     listen 443 ssl http2;
#     server_name mineradio.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/mineradio.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mineradio.yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:3000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
# }
